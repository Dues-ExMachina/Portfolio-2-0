# from fastmcp import FastMCP

# mcp = FastMCP("portfolio-mcp")

# @mcp.tool()
# def pinecone_retrieval(query: str) -> list[Chunk]:
#     " — semantic search over the Pinecone index, returns top-5 chunks with source metadata"



# @mcp.tool()
# `github_live(endpoint: str) -> dict:
# " — wraps GitHub REST API (repos, commits, README for a given repo, language stats). Needs a `GITHUB_TOKEN` env var."


# @mcp.tool()
# def web_fetch(url: str) -> str:
#     fetches text content of a live deployed URL (for when a visitor asks about a specific project's live features)


